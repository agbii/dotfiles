
- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

#- create:
#    - ~/Documents/Study
#    - ~/Documents/Personal

- shell:
    - 
      command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
#  - [git submodule update --init --recursive, Installing submodules]
